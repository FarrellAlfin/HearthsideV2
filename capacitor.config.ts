import type { CapacitorConfig } from '@capacitor/cli';

const config = {
  appId: 'com.hearthside.app',
  appName: 'Hearthside',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
